'use strict';
const{MatchApiError,assertSameOrigin,environment,identityForRequest,rpc}=require('./lib/match-service');
function sendJson(res,status,payload){res.statusCode=status;res.setHeader('content-type','application/json; charset=utf-8');res.setHeader('cache-control','no-store');res.setHeader('x-content-type-options','nosniff');res.end(JSON.stringify(payload))}
function bodyOf(req){if(!req.body)return{};if(typeof req.body==='object')return req.body;try{return JSON.parse(req.body)}catch{throw new MatchApiError('invalid_json',400)}}
module.exports=async function friendsHandler(req,res){if(req.method!=='POST'){res.setHeader('allow','POST');return sendJson(res,405,{ok:false,error:'method_not_allowed'})}try{assertSameOrigin(req);const env=environment(),identity=await identityForRequest(req,env),body=bodyOf(req),action=String(body.action||'');if(!/^[0-9a-f-]{36}$/i.test(identity.playerId))throw new MatchApiError('registered_account_required',403);let state;if(action==='list')state=await rpc('kantin_friend_state',{p_user_id:identity.playerId},env);else if(['request','accept','remove'].includes(action))state=await rpc(`kantin_friend_${action}`,{p_user_id:identity.playerId,p_other_identifier:String(body.userId||'')},env);else throw new MatchApiError('unknown_action',400);sendJson(res,200,{ok:true,...state})}catch(error){const known=error instanceof MatchApiError;if(!known)console.error('Kantin friends API failed',{message:error.message,stack:error.stack});sendJson(res,known?error.status:500,{ok:false,error:known?error.code:'friends_service_failed',message:known&&error.details?String(error.details):error.message})}}
module.exports._test={bodyOf};
