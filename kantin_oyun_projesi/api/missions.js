'use strict';
const{MatchApiError,assertSameOrigin,environment,identityForRequest,rpc}=require('./lib/match-service');
function sendJson(res,status,payload){res.statusCode=status;res.setHeader('content-type','application/json; charset=utf-8');res.setHeader('cache-control','no-store');res.setHeader('x-content-type-options','nosniff');res.end(JSON.stringify(payload))}
function bodyOf(req){if(!req.body)return{};if(typeof req.body==='object')return req.body;try{return JSON.parse(req.body)}catch{throw new MatchApiError('invalid_json',400)}}
async function state(identity,env){const missions=await rpc('kantin_mission_state',{p_player_id:identity.playerId},env);return Array.isArray(missions)?missions:[]}
module.exports=async function missionsHandler(req,res){if(req.method!=='POST'){res.setHeader('allow','POST');return sendJson(res,405,{ok:false,error:'method_not_allowed'})}try{assertSameOrigin(req);const env=environment(),identity=await identityForRequest(req,env),body=bodyOf(req),action=String(body.action||'');let claim=null;if(action==='claim')claim=await rpc('kantin_claim_mission',{p_mission_id:String(body.missionId||''),p_player_id:identity.playerId},env);else if(action!=='list')throw new MatchApiError('unknown_action',400);sendJson(res,200,{ok:true,missions:await state(identity,env),claim})}catch(error){const known=error instanceof MatchApiError;if(!known)console.error('Kantin missions API failed',{message:error.message,stack:error.stack});sendJson(res,known?error.status:500,{ok:false,error:known?error.code:'mission_service_failed',message:known&&error.details?String(error.details):error.message})}}
module.exports._test={bodyOf,state};
